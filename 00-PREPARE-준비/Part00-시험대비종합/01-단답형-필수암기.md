# 정보보안기사 실기 단답형 필수 암기

## 🔴 최신 기출 단답형 총정리 (2024년)

이 파일은 실제 기출 문제 기반으로 자주 출제되는 단답형 개념을 정리했습니다.

---

## 1. PAM 모듈 4가지 유형 ★★★

### 문제
```
PAM 모듈의 기능 중 아래 설명에 해당하는 모듈 유형을 각각 쓰시오.

ㄱ. 계정 인증
ㄴ. 권한 확인
ㄷ. 세션 연결
```

### 정답
```
ㄱ. auth
ㄴ. account (❌ permission 아님!)
ㄷ. session
```

### 4가지 유형 전체
```
auth     - 사용자 인증
account  - 계정/권한 확인
session  - 세션 설정
password - 패스워드 관리
```

---

## 2. Windows 기본 파일 시스템 ★★

### 문제
```
Windows NT 이상 버전에서 사용하는 기본 파일 시스템은?
```

### 정답
```
NTFS (New Technology File System)
```

### 추가 정보
```
FAT32  - 구형, 4GB 파일 크기 제한
NTFS   - 현대 Windows 표준 (권한 관리, 암호화 지원)
exFAT  - USB 등 외장 저장장치용
```

---

## 3. End-to-End 암호화 ★★★

### 문제
```
전송 시작부터 종료까지 암호화되어 중간에서 내용이 노출되지 않는 암호화 방식은?
```

### 정답
```
E2E 암호화 (End-to-End Encryption)
또는
종단간 암호화
```

### 설명
```
E2E 암호화:
- 송신자가 암호화 → 수신자가 복호화
- 중간 서버도 내용 볼 수 없음
- 예: WhatsApp, Signal, iMessage

구간 암호화 (Link Encryption):
- 구간별로 암호화/복호화 반복
- 서버에서 평문 노출 가능성

E2E가 더 안전!
```

---

## 4. DLP (Data Loss Prevention) ★★

### 문제
```
PC 등에 에이전트를 설치하여 정보 유출을 방지하는 엔드포인트 보안 솔루션은?
```

### 정답
```
DLP (Data Loss Prevention)
또는
데이터 유출 방지 시스템
```

### DLP 기능
```
1. 네트워크 DLP
   - 이메일, 메신저로 민감 정보 전송 차단

2. 엔드포인트 DLP
   - USB, 프린터 등 외부 반출 차단
   - 파일 복사/이동 제어

3. 저장소 DLP
   - DB, 파일 서버의 민감 정보 탐지
   - 주민번호, 신용카드 번호 등 자동 탐지
```

---

## 5. strcpy 보안 위협 ★★★

### 문제
```
strcpy를 사용하는 경우 발생할 수 있는 보안 위협은 무엇이며,
이 함수는 어떤 유형의 함수인가?
```

### 정답
```
보안 위협: 버퍼 오버플로우 (Buffer Overflow)

함수 유형: 비안전 함수 (Unsafe Function)
          또는 금지 함수 (Banned API)
```

### 안전한 대안
```
C/C++:
strcpy → strncpy, strcpy_s (bounds checking)
strcat → strncat, strcat_s
gets   → fgets
sprintf→ snprintf

위험한 함수들 (Banned Functions):
strcpy, strcat, gets, sprintf, scanf
```

---

## 6. Linux 패스워드 암호화 파일 ★★★

### 문제
```
리눅스에서 사용자 비밀번호가 암호화되어 저장되는 파일명은?
```

### 정답
```
/etc/shadow
```

### 관련 파일
```
/etc/passwd  - 사용자 계정 정보 (644 권한)
/etc/shadow  - 암호화된 패스워드 (400 권한) ★
/etc/group   - 그룹 정보
/etc/gshadow - 그룹 패스워드
```

---

## 7. DNS 프로토콜 및 TTL ★★★

### 문제
```
DNS가 사용하는 기본 프로토콜, 캐시 구조의 존재 여부,
TTL의 의미를 쓰시오.
```

### 정답
```
1. 프로토콜: UDP (일반 질의), TCP (Zone Transfer)
   - UDP 포트 53: 일반 DNS 질의/응답
   - TCP 포트 53: Zone Transfer (대량 데이터 전송)

2. 캐시 존재: 있음 (DNS Cache)
   - 로컬 DNS 서버가 응답 캐싱
   - 반복 질의 감소, 응답 속도 향상

3. TTL (Time To Live)
   - 캐시 유지 시간 (초 단위)
   - TTL=3600 → 1시간 동안 캐시 유지
   - TTL 만료 후 재질의
```

---

## 8. 웹셸 (Web Shell) ★★★

### 문제
```
공격자가 게시판에 업로드한 b.php 파일에서 cmd를 호출하는 코드가 있다.
이 파일의 목적은?
```

### 정답
```
웹셸 (Web Shell)
```

### 설명
```
웹셸:
- 웹 서버에 업로드된 악성 스크립트
- 공격자가 원격으로 서버 명령어 실행 가능
- 백도어 역할

파일 예시:
<?php system($_GET['cmd']); ?>

실행:
http://target.com/b.php?cmd=whoami
→ 서버에서 whoami 명령어 실행

대응:
- 파일 업로드 시 확장자 화이트리스트 검증
- 업로드 디렉토리 실행 권한 제거
- 정기적인 웹셸 탐지 (파일 무결성 검사)
```

---

## 9. 위험 분석 기법 3가지 ★★

### 문제
```
다음 설명에 해당하는 위험분석 기법을 쓰시오.

ㄱ. 일괄적 기준 적용
ㄴ. 자산별로 분석
ㄷ. 둘을 혼합
```

### 정답
```
ㄱ. 기준선 접근법 (Baseline Approach)
ㄴ. 상세 위험 분석 (Detailed Risk Analysis)
ㄷ. 복합 접근법 (Combined/Hybrid Approach)
```

### 설명
```
1. 기준선 접근법 (Baseline Approach)
   - 업계 표준 보안 통제 일괄 적용
   - 빠르고 경제적
   - 예: ISO 27001 Annex A 통제 항목 전체 적용

2. 상세 위험 분석 (Detailed Risk Analysis)
   - 자산별로 위험 평가 (자산, 위협, 취약점)
   - 정확하지만 시간/비용 많이 소요
   - 정량적 분석: ALE = SLE × ARO
   - 정성적 분석: 위험 매트릭스 (고/중/저)

3. 복합 접근법 (Combined/Hybrid)
   - 중요 자산: 상세 위험 분석
   - 일반 자산: 기준선 접근법
   - 실무에서 가장 많이 사용
```

---

## 10. 정보통신망 (법령 용어) ★

### 문제
```
법령상 '전송·처리' 등에 사용하는 네트워크를 지칭하는 용어는?
```

### 정답
```
정보통신망
```

### 관련 법령
```
정보통신망 이용촉진 및 정보보호 등에 관한 법률 (정보통신망법)

제2조(정의) 1항:
"정보통신망"이란 전기통신사업법 제2조제2호에 따른
전기통신설비를 이용하거나 전기통신설비와 컴퓨터 및
컴퓨터의 이용기술을 활용하여 정보를 수집·가공·저장·
검색·송신 또는 수신하는 정보통신체제를 말한다.
```

---

## 11. Apache 디렉토리 리스팅 방지 ★★

### 문제
```
Apache 설정에서 디렉토리 리스팅을 방지하기 위해
제거해야 하는 옵션은?
```

### 정답
```
Indexes
```

### 설정 예시
```
취약한 설정:
<Directory "/var/www/html">
    Options Indexes FollowSymLinks
    AllowOverride None
    Require all granted
</Directory>

안전한 설정:
<Directory "/var/www/html">
    Options -Indexes FollowSymLinks  # Indexes 제거 또는 -Indexes
    AllowOverride None
    Require all granted
</Directory>

설명:
Indexes 옵션 활성화 시:
→ index.html 없는 디렉토리에 접근하면 파일 목록 노출
→ 공격자가 파일 구조 파악 가능

-Indexes 설정 시:
→ 403 Forbidden 에러 반환
→ 디렉토리 내용 숨김
```

---

## 12. lastcomm (프로세스 계정 로그) ★★

### 문제
```
pacct 로그에서 마지막으로 실행된 명령어를 확인할 수 있는 명령어는?
```

### 정답
```
lastcomm
```

### 설명
```
lastcomm:
- Process Accounting 로그 조회 명령어
- 사용자가 실행한 명령어 이력 확인
- 감사 추적 (Audit Trail)에 사용

관련 파일:
/var/account/pacct  - 프로세스 계정 로그 (바이너리)
/var/log/account/   - 로그 저장 디렉토리

사용 예시:
lastcomm              # 전체 명령어 이력
lastcomm root         # root 사용자 명령어만
lastcomm ls           # ls 명령어 실행 이력
lastcomm | head -10   # 최근 10개

활성화:
systemctl start psacct  (CentOS/RHEL)
systemctl start acct    (Ubuntu/Debian)
```

---

## 13. CCTV 설치 법령 ★★

### 문제
```
사옥 외부 CCTV를 설치할 때 보안담당자가 해야 할 일은?
```

### 정답
```
개인정보보호법 제25조 (영상정보처리기기 설치·운영 제한)에 따라:

1. 안내판 설치 ★ (가장 중요)
   - 설치 목적
   - 촬영 범위
   - 촬영 시간
   - 관리책임자 성명 및 연락처
   - 잘 보이는 장소에 설치

2. 안내판 예외
   - 군사시설, 국가중요시설
   - 교정시설

내부 CCTV (출입통제 구역):
1. 촬영 사실 고지 및 동의
2. 음성 녹음 금지
3. 목적 외 사용 금지
4. 영상 안전한 보관 및 파기
```

---

## 14. 파일 업로드 취약점 우회 기법 ★★★

### 문제
```
if !array.contains(filetype) 조건으로 확장자를 검사하는
파일 업로드 기능의 취약점과 우회 기법을 설명하시오.
```

### 정답
```
취약점: 파일 업로드 취약점 (Unrestricted File Upload)

우회 기법:

1. 이중 확장자 (Double Extension)
   - shell.php.jpg
   - 일부 서버는 .php를 먼저 처리 → 실행됨

2. Null Byte Injection
   - shell.php%00.jpg
   - %00 이후 문자열 무시 → shell.php로 저장

3. 대소문자 우회
   - shell.PhP, shell.pHp
   - 대소문자 구분 안 하는 검증 로직 우회

4. Content-Type 조작
   - HTTP 헤더에서 Content-Type: image/jpeg
   - 실제 파일은 .php

5. Magic Number (파일 시그니처) 위조
   - JPEG 시그니처 추가: FF D8 FF
   - 뒤에 PHP 코드 삽입

방어 (3중 검사):
1. 확장자 화이트리스트 검증
2. MIME Type 검증
3. Magic Number 검증

+ 업로드 디렉토리 실행 권한 제거
+ 파일명 난수화 (UUID)
```

---

## 15. 위험 대응 4가지 ★★★

### 문제
```
정보보호 위험 대응 방법 4가지를 설명하시오.
```

### 정답
```
1. 위험 수용 (Risk Acceptance)
   - 위험 < DoA (허용 가능 수준)
   - 위험 감수하고 사업 진행
   - 경영진 승인 필요

2. 위험 감소/완화 (Risk Mitigation)
   - 보호대책 적용하여 위험 감소
   - 비용 대비 효과적인 경우
   - 보호대책 비용 + 잔여 위험 < 원래 위험

3. 위험 회피 (Risk Avoidance)
   - 위험이 너무 크고 대책 비용 과다
   - 사업/프로젝트 중단

4. 위험 전가 (Risk Transfer)
   - 타인에게 위험 전가
   - 보험 가입
   - 아웃소싱 (클라우드, MSP)
```

---

## 🎯 단답형 암기 전략

### 암기 우선순위

**★★★ 최우선 (매년 출제)**
```
1. /etc/shadow
2. PAM 모듈 (auth, account, session, password)
3. E2E 암호화
4. 웹셸
5. 버퍼 오버플로우
```

**★★ 자주 출제**
```
6. NTFS
7. DNS (UDP/TCP, TTL)
8. DLP
9. 위험 분석 기법 (기준선, 상세, 복합)
10. 위험 대응 4가지
```

**★ 가끔 출제**
```
11. Apache Indexes
12. lastcomm
13. CCTV 법령
14. 파일 업로드 우회
15. 정보통신망
```

---

## 💡 실기 시험 팁

### 1. 파일 경로는 절대 경로로
```
✅ /etc/shadow
❌ shadow
❌ /shadow
```

### 2. 약자는 풀어쓰기도 준비
```
DLP = Data Loss Prevention
E2E = End-to-End
TTL = Time To Live
```

### 3. 법령 문제는 정확한 용어로
```
✅ 정보통신망
❌ 네트워크
❌ 통신망
```

### 4. 보안 위협은 영문+한글
```
✅ 버퍼 오버플로우 (Buffer Overflow)
✅ MITM (Man-In-The-Middle)
```

---

## 🔥 2024년 실제 기출 기반 핵심 암기

**이 15개 개념만 완벽히 외우면 단답형 80% 커버!**

1. PAM: auth, **account**, session, password
2. NTFS
3. **E2E 암호화**
4. DLP
5. 버퍼 오버플로우 + strcpy
6. **/etc/shadow**
7. DNS: UDP(53), TCP(53), DNS 캐시, **TTL**
8. **웹셸**
9. 기준선법, 상세위험분석, 복합
10. 정보통신망
11. Apache **Indexes** (디렉토리 리스팅)
12. **lastcomm**
13. 위험 대응 4가지
14. 파일 업로드 우회 (이중확장자, Null Byte)
15. CCTV 안내판 (개인정보보호법 제25조)
