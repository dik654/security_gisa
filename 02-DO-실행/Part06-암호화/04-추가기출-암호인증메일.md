# 추가 기출 - 암호/인증/메일 핵심 정리

## 🔴 HIGHT 암호 ★★★

### 문제
```
암호화 알고리즘 중 매핑이 다른 것은?
HIGHT - 공개키 암호화 ← 틀림!
```

### ✅ 정답
```
HIGHT: 대칭키 블록 암호 ★★★

HIGHT (High security and light weight)
- 한국 표준 (TTAS)
- 64bit 블록, 128bit 키
- 경량 암호 (IoT, RFID)
- 대칭키 암호! (공개키 아님!)
```

---

## 🔴 Rabin 암호 ★★

### 정답
```
Rabin 암호화 알고리즘

특징:
- 공개키 암호 (비대칭키)
- RSA와 유사
- 소인수분해 기반
- Rabin이 만듦 (M. Rabin) ★★

vs RSA:
┌────────┬──────────┬──────────┐
│  구분  │   RSA    │  Rabin   │
├────────┼──────────┼──────────┤
│ 기반   │소인수분해│소인수분해│
├────────┼──────────┼──────────┤
│ 복호화 │ 1개 평문 │ 4개 후보 │
│        │          │ (단점)   │
└────────┴──────────┴──────────┘
```

---

## 🔴 RIPEMD-160 ★★

### 문제
```
유럽에서 고안된 160비트 해시 함수는?
```

### ✅ 정답
```
RIPEMD-160 ★★★

RIPEMD (RACE Integrity Primitives Evaluation Message Digest)
- 160bit 출력
- 유럽에서 개발
- MD4/MD5 대안
- SHA-1과 유사한 보안 수준
```

---

## 🔴 Diffie-Hellman (DH) ★★★

### 정답
```
Diffie-Hellman 키 교환

목적: 안전하지 않은 채널에서 비밀키 공유 ★★★

동작:
1. A와 B가 공개 파라미터 합의 (p, g)
2. A: 비밀값 a 선택, A = g^a mod p 계산 → B에게 전송
3. B: 비밀값 b 선택, B = g^b mod p 계산 → A에게 전송
4. A: K = B^a mod p = g^ab mod p 계산
5. B: K = A^b mod p = g^ab mod p 계산
6. 공유 비밀키 K = g^ab mod p ★★★

장점:
- 중간자가 g^a, g^b를 알아도 g^ab 계산 불가 (이산로그 난제)

단점:
- 중간자 공격 (MITM) 취약
- 인증 필요 (디지털 서명)

용도:
- SSL/TLS 키 교환
- VPN (IKE)
```

---

## 🔴 블록 암호 운영 모드 ★★★

### 문제
```
블록 암호 운영 모드 암호화 과정 (그림)
```

### ✅ 정답
```
주요 모드:

1. ECB (Electronic Codebook) ★★
   평문 블록 → 암호화 → 암호문 블록
   (블록마다 독립적)

   단점: 패턴 노출! (보안 취약)

   그림:
   P1 → [E] → C1
   P2 → [E] → C2
   P3 → [E] → C3

2. CBC (Cipher Block Chaining) ★★★
   이전 암호문 블록과 XOR 후 암호화

   IV ⊕ P1 → [E] → C1
   C1 ⊕ P2 → [E] → C2
   C2 ⊕ P3 → [E] → C3

   특징:
   - IV (초기화 벡터) 필요
   - 병렬 처리 불가 (순차적)
   - 가장 많이 사용 ★★★

3. CTR (Counter) ★★
   카운터를 암호화하여 평문과 XOR

   Counter1 → [E] → ⊕ P1 → C1
   Counter2 → [E] → ⊕ P2 → C2

   특징:
   - 병렬 처리 가능
   - 스트림 암호처럼 동작

비교:
┌──────┬────────┬──────────┐
│ 모드 │ 보안   │   특징   │
├──────┼────────┼──────────┤
│ ECB  │ 취약 ✗│ 패턴 노출│
├──────┼────────┼──────────┤
│ CBC  │ 안전 ✓│ 가장 일반│
│      │        │  적 ★★★│
├──────┼────────┼──────────┤
│ CTR  │ 안전 ✓│ 병렬 가능│
└──────┴────────┴──────────┘
```

---

## 🔴 WEP (RC4) ★★

### 문제
```
WEP에서 사용하는 암호 알고리즘은?
```

### ✅ 정답
```
RC4 (40비트 또는 104비트 키) ★★★

WEP (Wired Equivalent Privacy)
- 무선 LAN 초기 암호화 프로토콜
- RC4 스트림 암호 사용
- IV (24bit) + 키 (40/104bit)

취약점:
- IV가 짧아 재사용 ★★
- Key stream 공격
- 몇 분 만에 크랙 가능

현재:
- 사용 금지!
- WPA2/WPA3 사용 ★★★
```

---

## 🔴 CPA/CCA/COA 공격 ★★

### 문제
```
공격자가 평문을 선택하면 암호문을 얻을 수 있는 공격은?
```

### ✅ 정답
```
CPA (Chosen Plaintext Attack) = 선택 평문 공격 ★★★

공격 분류:

1. COA (Ciphertext Only Attack)
   - 암호문만 가지고 공격
   - 가장 어려움

2. KPA (Known Plaintext Attack)
   - 평문-암호문 쌍을 알고 있음
   - 에니그마 해독에 사용

3. CPA (Chosen Plaintext Attack) ★★★
   - 공격자가 평문 선택
   - 해당 암호문 획득
   - 암호 시스템 분석

4. CCA (Chosen Ciphertext Attack)
   - 공격자가 암호문 선택
   - 해당 평문 획득 (복호화 오라클)

난이도: COA > KPA > CPA > CCA
(COA가 가장 어려움)
```

---

## 🔴 Kerberos v4 vs v5 ★★★

### 문제
```
Kerberos v4와 v5 비교 중 틀린 설명은?
```

### ✅ 정답
```
Kerberos v5 개선사항:

┌──────────────┬────────┬────────┐
│    항목      │   v4   │   v5   │
├──────────────┼────────┼────────┤
│ 암호 알고리즘│ DES만  │ 다양 ★│
├──────────────┼────────┼────────┤
│ 티켓 수명    │ 고정   │ 가변 ★│
├──────────────┼────────┼────────┤
│ IP 주소      │ 포함 ★│ 선택적 │
│ (티켓에)     │ (NAT   │        │
│              │ 문제)  │        │
├──────────────┼────────┼────────┤
│ 인증 전달    │ 불가   │ 가능 ★│
│ (Delegation) │        │        │
└──────────────┴────────┴────────┘

주요 차이:
- v5는 암호 방식 선택 가능 (AES 등)
- v5는 티켓 갱신 (Renewable) 지원
- v5는 NAT 환경 지원
```

---

## 🔴 802.1X / EAP ★★★

### 문제
```
무선랜 802.1X 기반 인증 수단은?
```

### ✅ 정답
```
EAP (Extensible Authentication Protocol) ★★★

802.1X 구성:
- Supplicant (클라이언트)
- Authenticator (AP/스위치)
- Authentication Server (RADIUS)

EAP 종류:
- EAP-TLS: 인증서 기반 (가장 안전) ★★
- EAP-TTLS: 서버만 인증서
- PEAP: Protected EAP
- EAP-MD5: 약함 (사용 금지)

용도:
- 무선 LAN (WPA2-Enterprise) ★★★
- 유선 LAN (Port 기반 인증)
```

---

## 🔴 SET (전자상거래) ★★

### 문제
```
SET에서 중요하게 다뤄지는 기술 2가지는?
```

### ✅ 정답
```
1. 전자봉투 (Digital Envelope) ★★★
2. 이중서명 (Dual Signature) ★★★

SET (Secure Electronic Transaction)
- 신용카드 전자상거래 프로토콜

전자봉투:
대칭키로 데이터 암호화
→ 대칭키를 수신자 공개키로 암호화
→ 암호화된 데이터 + 암호화된 대칭키 전송

이중서명:
주문 정보 (OI)와 결제 정보 (PI) 분리
→ 가맹점은 OI만, 은행은 PI만 볼 수 있음
→ 프라이버시 보호 ★★
```

---

## 🔴 인증서 만료 시 CA 역할 ★★

### 문제
```
인증서 만료 시 CA의 역할은?
```

### ✅ 정답
```
인증서 만료 시 CA 역할:

1. CRL (Certificate Revocation List) 업데이트
   - 만료된 인증서 목록에 추가 ★★

2. OCSP 응답
   - "revoked" 또는 "expired" 상태 반환

3. 갱신 (Renewal) 처리
   - 사용자 요청 시 새 인증서 발급
   - 신원 재확인

4. 통지
   - 만료 전 경고 (30일, 7일 전 등)

주의:
만료 ≠ 폐기
- 만료: 유효기간 지남 (자동)
- 폐기: 강제로 무효화 (CRL/OCSP)
```

---

## 🔴 autologon (Windows) ★

### 정답
```
Windows 자동 로그온 설정

레지스트리:
HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon

DefaultUserName: 사용자명
DefaultPassword: 비밀번호
AutoAdminLogon: 1 (활성화)

보안 위험:
- 비밀번호 레지스트리에 저장 ★★
- 물리적 접근 시 무단 사용
- 권장하지 않음

대안:
- 화면 보호기 비밀번호
- 빠른 사용자 전환
```

---

## 🔴 메일 서버 구성 ★★★

### 문제
```
메일 서버 요소 중 틀린 것은?
```

### ✅ 정답
```
메일 시스템 구성:

1. MUA (Mail User Agent)
   - 사용자 메일 클라이언트
   - Outlook, Thunderbird

2. MTA (Mail Transfer Agent) ★★★
   - 메일 전송 서버
   - Sendmail, Postfix
   - SMTP 사용

3. MDA (Mail Delivery Agent)
   - 메일 전달 (수신함에 저장)
   - Procmail

4. POP3 / IMAP
   - 메일 수신 프로토콜
   - POP3: 다운로드 후 삭제
   - IMAP: 서버에 보관 ★★

포트:
- SMTP: 25 (평문), 587 (TLS), 465 (SSL)
- POP3: 110 (평문), 995 (SSL) ★★
- IMAP: 143 (평문), 993 (SSL)

틀린 예:
"3POP" ← 틀림! POP3가 맞음
"SMTP는 수신 프로토콜" ← 틀림! 송신
```

---

## 🔴 S/MIME ★★★

### 문제
```
X.509 인증서를 사용하는 메일 보안 기술은?
```

### ✅ 정답
```
S/MIME (Secure/Multipurpose Internet Mail Extensions) ★★★

기능:
1. 전자서명
   - 발신자 인증
   - 부인방지
   - 무결성 보장

2. 암호화
   - 기밀성 보장

특징:
- X.509 인증서 사용 ★★★
- MIME 표준 확장
- Outlook, Thunderbird 지원

vs PGP:
┌───────┬──────────┬──────────┐
│ 구분  │  S/MIME  │   PGP    │
├───────┼──────────┼──────────┤
│인증서 │ X.509 ★ │ 자체 키링│
├───────┼──────────┼──────────┤
│ CA    │ 필요     │ 불필요   │
│       │          │(신뢰망)  │
└───────┴──────────┴──────────┘
```

---

## 🔴 이메일 스푸핑 ★★

### 문제
```
합법적인 발신자로 위장하여 발신자 주소를 조작하는 공격은?
```

### ✅ 정답
```
이메일 스푸핑 (Email Spoofing) ★★★

원리:
- SMTP는 발신자 인증 없음
- From 헤더 쉽게 위조 가능

공격 예:
From: ceo@company.com (위조)
To: employee@company.com
제목: 긴급! 송금 요청

방어:

1. SPF (Sender Policy Framework) ★★★
   - DNS에 허가된 메일 서버 IP 등록
   - 수신 서버가 발신 IP 검증

2. DKIM (DomainKeys Identified Mail) ★★
   - 발신 서버가 디지털 서명
   - 수신 서버가 서명 검증

3. DMARC (Domain-based Message Authentication)
   - SPF + DKIM 정책 통합
   - 실패 시 처리 방법 지정 (거부/격리)

4. 사용자 교육
   - 발신자 주소 확인
   - 의심스러운 링크 클릭 금지
```

---

## 🔴 URL 스푸핑 ★★

### 문제
```
URL 스푸핑이란?
```

### ✅ 정답
```
URL 스푸핑 (URL Spoofing)

정의:
주소 표시줄에 가짜 URL을 표시하여 사용자를 속이는 기법

방법:

1. 유사 도메인
   - paypal.com → paypa1.com (1은 숫자)
   - google.com → goog1e.com

2. 서브도메인 악용
   - paypal.com.phishing.com
   - (paypal.com이 서브도메인!)

3. Punycode (IDN Homograph)
   - аррӏе.com (키릴 문자)
   - apple.com처럼 보임

4. @기호 악용
   - http://google.com@evil.com
   - (실제 접속: evil.com)

5. 긴 URL
   - http://bank.com/../../../evil.com/...

방어:
- HTTPS 확인 ★★★
- EV SSL 인증서 (녹색 바)
- 주소 직접 입력
- 북마크 사용
- 피싱 필터 (브라우저)
```

---

## 🎯 핵심 암기

```
HIGHT: 대칭키 (공개키 아님!) ★★★
Rabin: 공개키, RSA 유사
RIPEMD-160: 유럽, 160bit 해시
Diffie-Hellman: 키 교환 ★★★

블록암호 모드:
- ECB: 패턴 노출 (취약)
- CBC: 가장 일반적 ★★★

WEP: RC4 (취약, 사용 금지)
CPA: 선택 평문 공격 ★★★

Kerberos v5: 암호 다양, 티켓 가변
802.1X: EAP 인증 ★★★
SET: 전자봉투 + 이중서명 ★★★

S/MIME: X.509 메일 보안 ★★★
이메일 스푸핑: SPF/DKIM/DMARC
URL 스푸핑: HTTPS, EV SSL
```
